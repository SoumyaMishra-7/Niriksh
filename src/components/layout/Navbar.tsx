import { useState } from 'react'
import { AnimatePresence, motion } from 'framer-motion'
import { Menu, X, ArrowUpRight } from 'lucide-react'
import { navItems } from '../../data/content'
import { useScroll } from '../../hooks/useScroll'
export function Logo({light=false}:{light?:boolean}) { return <a href="#top" className={`logo ${light?'light':''}`} aria-label="Niriksh home"><span className="logo-mark">N</span><span>Niriksh</span></a> }
export function Navbar(){ const [open,setOpen]=useState(false); const scrolled=useScroll(); return <header className={`navbar ${scrolled?'scrolled':''}`}><div className="nav-inner"><Logo/><nav className="desktop-nav" aria-label="Primary">{navItems.map(([label,id])=><a key={id} href={`#${id}`}>{label}</a>)}</nav><div className="nav-actions"><a href="#signin" className="signin">Sign in</a><a href="#platform" className="nav-cta">Explore platform <ArrowUpRight size={15}/></a></div><button className="menu-button" onClick={()=>setOpen(!open)} aria-label={open?'Close menu':'Open menu'} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.nav className="mobile-nav" aria-label="Mobile" initial={{opacity:0,y:-12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:-12}}>{navItems.map(([label,id])=><a key={id} href={`#${id}`} onClick={()=>setOpen(false)}>{label}<ArrowUpRight size={17}/></a>)}<a className="mobile-primary" href="#platform" onClick={()=>setOpen(false)}>Explore platform</a></motion.nav>}</AnimatePresence></header> }
