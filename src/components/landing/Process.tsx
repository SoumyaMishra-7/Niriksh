import { motion } from 'framer-motion'
import { Eye, BrainCircuit, Orbit, Zap, ArrowRight } from 'lucide-react'
import { SectionHeading } from '../ui/SectionHeading'
const steps=[[Eye,'SEE','Detect what is happening.'],[BrainCircuit,'UNDERSTAND','Interpret store conditions.'],[Orbit,'PREDICT','Estimate what happens next.'],[Zap,'ACT','Recommend the best action.']] as const
export function Process(){return <section id="how-it-works" className="process"><div className="section"><SectionHeading eyebrow="THE NIRIKSH DIFFERENCE" title="From seeing problems to preventing them." copy="A continuous intelligence loop turns live store conditions into timely action."/><div className="process-grid">{steps.map(([Icon,title,desc],i)=><div className="process-step" key={title}><motion.div className="process-orb" initial={{scale:.75,opacity:0}} whileInView={{scale:1,opacity:1}} viewport={{once:true}} transition={{delay:i*.14}}><Icon size={22}/><span>{i+1}</span></motion.div><div><h3>{title}</h3><p>{desc}</p></div>{i<3&&<ArrowRight className="process-arrow"/>}</div>)}</div><div className="scenario-grid"><Scenario label="SHELF AVAILABILITY" value="17%" prediction="Stock-out predicted in 24 min" action="Replenish Shelf A3"/><Scenario label="QUEUE LENGTH" value="6" prediction="Congestion predicted in 7 min" action="Open Counter 4"/></div></div></section>}
function Scenario({label,value,prediction,action}:{label:string;value:string;prediction:string;action:string}){return <motion.article className="scenario" initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true}}><div><span>{label}</span><strong>{value}</strong></div><ArrowRight/><div><span>PREDICTION</span><b>{prediction}</b></div><ArrowRight/><div className="scenario-action"><span>RECOMMENDED ACTION</span><b>{action}</b></div></motion.article>}
