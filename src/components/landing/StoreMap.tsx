import { useState } from 'react'
import { motion } from 'framer-motion'
import { Activity, Users } from 'lucide-react'
import { zones } from '../../data/content'
import { SectionHeading } from '../ui/SectionHeading'
export function StoreMap(){const [active,setActive]=useState(zones[3]);return <section id="platform" className="section store-section"><SectionHeading eyebrow="LIVE STORE INTELLIGENCE" title="One view of everything that matters." copy="Explore a privacy-safe operational map built from anonymous edge events."/><div className="store-console"><div className="console-bar"><div><i/> STORE 01 · LIVE</div><span>Updated just now</span></div><div className="store-body"><div className="interactive-map">{zones.map(z=><button key={z.id} onMouseEnter={()=>setActive(z)} onFocus={()=>setActive(z)} onClick={()=>setActive(z)} className={`map-zone ${z.grid} ${active.id===z.id?'active':''}`}><span>{z.name}</span><small>{z.shoppers} live</small></button>)}<div className="map-heat mh1"/><div className="map-heat mh2"/><div className="map-heat mh3"/></div><motion.aside className="zone-detail" key={active.id} initial={{opacity:0,x:8}} animate={{opacity:1,x:0}}><div className="detail-top"><span>SELECTED ZONE</span><Activity size={18}/></div><h3>{active.name}</h3><div className="engagement"><span><Users size={16}/> Current shoppers</span><b>{active.shoppers}</b></div><dl><div><dt>Average dwell</dt><dd>{active.dwell}</dd></div><div><dt>Traffic</dt><dd className="positive">{active.traffic}</dd></div></dl><div className="status-label"><i/> {active.status}</div><small>Anonymous, temporary tracking only</small></motion.aside></div></div></section>}
