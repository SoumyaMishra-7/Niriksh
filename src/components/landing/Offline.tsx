import { Wifi, WifiOff, Check, CloudCog, ArrowRight } from 'lucide-react'
import { SectionHeading } from '../ui/SectionHeading'
export function Offline(){return <section className="section offline"><SectionHeading eyebrow="RESILIENT BY DEFAULT" title="Intelligence that doesn’t disappear with your internet." copy="The edge keeps working. Your store keeps understanding. Sync simply catches up when the connection returns."/><div className="connection-grid"><Connection online/><Connection online={false}/></div><div className="restore"><CloudCog/><span>Connection restored</span><ArrowRight/><b>Metadata automatically synchronised</b></div></section>}
function Connection({online}:{online:boolean}){const list=online?['Edge analytics running','Metadata sync active','Dashboard updated']:['Edge analytics still running','Alerts still generated','Events stored locally'];return <article className={`connection ${online?'online':''}`}><div className="connection-head">{online?<Wifi/>:<WifiOff/>}<div><span>INTERNET</span><h3>{online?'Online':'Offline'}</h3></div><i/></div><ul>{list.map(x=><li key={x}><Check/>{x}</li>)}</ul>{!online&&<div className="paused">Cloud sync paused · safely queued</div>}</article>}
