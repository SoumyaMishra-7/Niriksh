import { Activity,Camera,Users,AlertTriangle } from 'lucide-react'
import { DashboardMap } from '../../components/dashboard/DashboardMap'
import { KpiCard } from '../../components/dashboard/KpiCard'
import { Card,PageHeader,SectionHeader,StatusBadge } from '../../components/dashboard/ui'
import { alerts } from '../../data/alerts'
export default function LiveStore(){return <><PageHeader eyebrow="LIVE STORE" title="Store intelligence map" copy="A real-time, privacy-safe view of occupancy, flow and operational exceptions." actions={<StatusBadge status="normal" label="Live analytics running"/>}/><div className="compact-kpis"><KpiCard label="Active Customers" value="46" note="5 zones" icon={Users}/><KpiCard label="Cameras Online" value="14/15" note="1 needs attention" icon={Camera}/><KpiCard label="Operational Alerts" value="5" note="2 critical" icon={AlertTriangle} tone="red"/><KpiCard label="Events / min" value="238" note="Edge processed" icon={Activity}/></div><section className="overview-section"><SectionHeader title="Interactive floor map" copy="Select a zone to inspect live context."/><Card><DashboardMap expanded/></Card></section><section className="overview-section"><SectionHeader title="Live operational alerts"/><Card className="alert-list">{alerts.map(a=><div key={a.id}><StatusBadge status={a.severity}/><div><b>{a.title}</b><small>{a.location}</small></div><span>{a.time}</span><button>Inspect →</button></div>)}</Card></section></>}
