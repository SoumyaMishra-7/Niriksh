# Models

Model binaries are intentionally excluded. Pass a local nano-class YOLO model using `--model path/to/model.pt`. Generic COCO person detection is suitable only for anonymous shopper/queue measurement; shelf/SKU claims require retailer-specific validation.
